name: Push Main

on:
  push:
    branches: [main]

jobs:
  call-format:
    uses: ./.github/workflows/format.yml

  call-test:
    uses: ./.github/workflows/test.yml
    secrets: inherit

  call-publish:
    needs: [call-format, call-test]
    uses: ./.github/workflows/test.yml
    permissions:
      contents: read
      id-token: write
